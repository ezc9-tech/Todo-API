# Todo-API

This is a todo app api so that I can learn about basic REST API concepts. It uses node.js, mongoDB, mongoose, and express.

You can format using npx prettier . --write
You can run in dev enviorment using npm run dev
